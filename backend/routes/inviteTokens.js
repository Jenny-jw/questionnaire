import express from "express";

const router = express.Router();

// POST /api/inviteToken/verify, verify the token after invitee input
// OTP used by fillers, Node. crypto or uuid

const inviteTokensRouter = router;
export default inviteTokensRouter;
